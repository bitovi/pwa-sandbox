export const base = 8

export const values = [
  0,
  base * 1,
  base * 2,
  base * 3,
  base * 4,
  base * 5,
  base * 6,
  base * 7,
  base * 8,
  base * 9,
  base * 10,
  base * 11,
  base * 12,
]

// values[0.125] = base * 0.125
// values[0.25] = base * 0.25
// values[0.5] = base * 0.5
// values[1.5] = base * 1.5
// values[2.5] = base * 2.5
// values[3.5] = base * 3.5

export default {
  base,
  values,
}
